import db from "../config/Database.js";
const createViewSql = `

`;

// db.query(createViewSql).then(() => { 
//   console.log('View created successfully!');
// }).catch(error => {
//   console.error('Failed to create view:', error);
// });
export default createViewSql;